# B2 BLOCK-001 快速复核

## 目录

- [1. 结论](#1-结论)
- [2. 验证结果](#2-验证结果)
- [3. 边界判断](#3-边界判断)
- [4. 下一步](#4-下一步)

## 1. 结论

**B2 `BLOCK-001` 快速功能验证通过，放行 B3；不启动跨平台、Benchmark 或完整 Hardening。**

这次复核以真实 Toolkit RPK、真实 JS/Core/LVGL/SDL 运行和资源清理为门禁，不要求额外身份观测开发。

## 2. 验证结果

### Toolkit

```text
npm test
80 passed, 0 failed
```

已生成真实 `tk-s10-block001.rpk`，SHA-256：

```text
7638b22e2c89af02ebf363d3b09540f08079e4985deb5681a9fcc9d33ea1124e
```

### LVGL/SDL

```text
cmake --build build-m1-s2 -j4
SDL_VIDEODRIVER=dummy ./build-m1-s2/quickapp_case001_lvgl --block-001
```

结果退出码为 `0`：

- `[A,B] -> [A,B,C]`：只 Instantiate C，A/B 保持。
- `[A,B,C] -> [A,C]`：Remove B、Move C，B 的 Handler/Node/NativeHandle 释放。
- `[A,C] -> [A,B,C]`：重新创建 B，获得新 Block/Node/Handler/NativeHandle。
- 旧 Handler 返回 `HANDLER_NOT_FOUND`。
- Revision `0 -> 1 -> 2 -> 3` 全部 presented。
- Runtime、Platform、JS 资源 teardown 后归零。

## 3. 边界判断

- 修复 block-scoped Handler 归属属于 B2 必需的生命周期适配，没有改变公共 ABI。
- 没有修改 Android、iOS、Benchmark 或公共 Contract。
- B2 不证明嵌套 Block、跨父节点 Move、Block 内部数据绑定变化和完整故障矩阵；这些留在后续 Hardening。
- Phase 2 仍未完成。

## 4. 下一步

立即放行 `B3`，目标是 `Image`、`Input` 和基础样式的最小真实运行闭环；继续只使用 LVGL/SDL，不修改 Android/iOS/Benchmark。
