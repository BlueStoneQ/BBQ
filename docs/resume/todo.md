# Resume 排雷清单

> 简历上写了但细节掌握不到位的点，面试前集中处理。
> 处理完把原因、方案、关键数据补到 resume-draft-explain.md。

---

## XM 负载性能分析平台

### 大文件上传（MPU 分片并发）

- 选型理由：为什么 S3 MPU 而不是自定义分片 / 整传？（已有资料，explain 里有）
- 分片大小为什么 64MB？（已答：2 的幂次对齐，避免跨边界开销）
- 并发数怎么定的？（需要确认：6 并发是怎么得出的？带宽/CPU 瓶颈分析过吗？）
- 失败重试机制？（单个分片失败怎么处理？）
- 合并逻辑：S3 的 complete 流程？

### ECharts 大数据渲染优化

- 具体用了哪些手段？Web Worker + ？需要确认：
  - [ ] 是否用了数据降采样？算法是 LTTB 还是其他？
  - [ ] 是否开启了 ECharts large 模式？
  - [ ] 数据量级是多少？（几千、几万、几十万数据点？）
  - [ ] 优化前后的性能数据（FPS / 首屏时间）？
- Web Worker 具体做什么？数据聚合？格式转换？

---

## XM 快应用框架

### 自动化测试（待补充到简历）

- Python 脚本自动化操作手机进行测试
- 需要补充细节：
  - [ ] 用了什么库？（Appium / uiautomator2 / adb？）
  - [ ] 测试范围：功能测试 / 性能测试 / 稳定性测试？
  - [ ] 脚本规模、执行频率、覆盖场景
  - [ ] 解决了什么痛点？人工测试的对比数据？
  - [ ] 和 CI/CD 的集成方式？
- 排雷完后加到简历 XM 快应用框架项目下

### DEX Layout Optimization 数据补充

- 简历写了"启动内存优化"，但缺具体数据
- 需要补充：
  - [ ] 启动耗时优化前后对比（冷启动时间 ms）
  - [ ] 内存占用优化前后对比（PSS/RSS）
  - [ ] 首屏渲染时间变化
- 数据补上后加到简历描述里

### 签名机制（IDE 构建打包工具）

- 简历写了"rpk 编译 + 签名"，签名部分不熟
- 需要确认：
  - [ ] rpk 签名用的什么算法？
  - [ ] 签名流程是怎样的？
  - [ ] 签名在 IDE 里是自动还是手动？
- 如果不熟，简历暂不加签名相关描述

### CI/CD 构建环境修复

- 目前只记得更新了 Node.js 版本
- 需要补充：还修复了什么？当时构建失败的具体报错？
- 如果只是升级 Node，简历保持现状"CI/CD 构建环境修复"一句话带过

---

## XT App

### 一键注册 RN 混合原生开发

- 简历写了"一键获取用户手机号"
- 需要确认：
  - [ ] 底层用的是哪个运营商 SDK？（移动认证/联通认证/电信认证？）
  - [ ] RN 侧怎么封装的 Native Module？
  - [ ] Android 和 iOS 的实现差异？

---

## 规则

1. 每个雷点排完后，在 resume-draft-explain.md 里补充完整的 Q&A
2. 如果排不掉（真没做过/记不清），从 draft-v2 里去掉该点，不硬撑
3. 排雷优先级：MPU 上传 > ECharts > 一键注册 > 签名 / CI 修复
