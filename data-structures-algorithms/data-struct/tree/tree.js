/**
 * 二叉树Tree
 * 1. 思考 + 会议：自己实现一遍
 */

 /**
  * 节点class定义
  */
 class Node {
   /**
    * @param {*} key 该节点存储的值
    */
   constructor(key) {
     this.key = key;
     this.left = none;
     this.right = none;
   }
 }

  /**
   * 树的class定义
   */
  class Tree {
    /** 
     * @param {*} root  根节点的值
     */
    constructor(root) {
      this.root = root;
    }
  }

  /**
   * 初始化（构建）一棵树
   */
  

  /**
   * 遍历
   */

  /**
   * 插入
   */

  /**
  * 删除
  */